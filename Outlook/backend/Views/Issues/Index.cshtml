@model IEnumerable<backend.Models.Issue>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IssueNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ar_pdf)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.en_pdf)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ar_cover)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.en_cover)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IssueNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ar_pdf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.en_pdf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ar_cover)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.en_cover)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
